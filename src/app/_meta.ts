import type { MetaRecord } from 'nextra';

const meta: MetaRecord = {
    'getting-started': 'Getting Started',
    index: {
        type: 'doc',
        display: 'hidden',
    },
};

export default meta;
